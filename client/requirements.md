## Packages
framer-motion | For smooth page transitions and micro-interactions
date-fns | For formatting dates in report lists

## Notes
Authentication uses session-based auth with `credentials: "include"`
Reports have roles: 'reporter' can only create/view own, 'admin' can view all/update status
Status workflow: pending -> reviewed -> closed
